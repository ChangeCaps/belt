while true {
	let index = 0;
	let crates = crates();

	while index != len(crates) {
		if crates[index].type == "crate_1" {
			let id = crates[index].id;
			print(id);
			grab(id);
			drop(1, 1);
		}

		index = index + 1;
	}
}